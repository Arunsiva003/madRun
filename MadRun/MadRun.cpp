#include "MadRun.h";

dataframe::DataFrame dataframe::read(){
    //read 
        //vector - pushData
        std::vector<std::any> push;
        dataframe::DataFrame newData(push);
        return newData;
}
